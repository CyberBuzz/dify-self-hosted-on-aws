  # AWS Account & Region (required for domain/email features)
  CDK_DEFAULT_ACCOUNT=
  CDK_DEFAULT_REGION=ap-northeast-1

  # Domain configuration
  DIFY_DOMAIN_NAME=
  DIFY_SUBDOMAIN=dify

  # Console IP restriction (comma-separated CIDRs, optional)
  CONSOLE_ALLOWED_CIDRS=
  EOF